var PI=1,factorial=1,cont=2,x=4,masm=1,inf=6;   
var x=Math.pow (numero,con);
var factorial=1;
console.log(pi(numero,x,cont));

        for(i=1;i<=inf;i++) 

function pi(numero,x,cont);
{
    pi+pi(x-1,cont*masm);
    cont +=2;
    masm*=1;

        for(i=1;i<=factorial;i++)
{
    factorial*=i;
}
    x=x/factorial;
        return x;
}