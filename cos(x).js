var cos=1,factorial=1,cont=2,x=3,masm=1,inf=6;   
var x=Math.pow (numero,con);
var factorial=1;
console.log(cos(numero,x,cont));

        for(i=1;i<=inf;i++) 

function cos(numero,x,cont);
{
    cos+cos(x,cont*masm);
    cont +=2;
    masm*=1;

        for(i=1;i<=factorial;i++)
{
    factorial*=i;
}
    x=x/factorial;
        return x;
}